# OS / editor
.DS_Store
*.pem

# env files (can opt-in for committing if needed)
.env*
!.env.example

# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# VSCode: settings.json はマシン固有の絶対パス（JDKパス等）を含むため非管理
# extensions.json はマシン非依存のため管理対象
.vscode/settings.json

# curl動作確認用のCookie jar（実際のJWTを含むため非管理）
cookie.txt

# 個人用の実装ナレッジメモ（非公開、git管理対象外）
docs/auth-implementation-notes.md
